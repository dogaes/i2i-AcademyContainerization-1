# i2i-AcademyContainerization-1
Created an index.html file, typing out "Hello from i2i Academy Docker Container!"
Wrote a docker-compose.yml file using the nginx:latest image. Configured the service to serve your custom index.html file and expose it on port 8080.
Deploy the container and verify


Replicated the exact setup on Cloud VM
